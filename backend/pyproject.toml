[project]
name = "insyt-backend"
version = "0.1.0"
description = "Insyt context-intelligence platform — backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
